{
  "name": "pixel-weather-server",
  "version": "1.0.0",
  "description": "Push notification server for Pixel Weather",
  "main": "api/send-notification.js",
  "dependencies": {
    "expo-server-sdk": "^3.15.0"
  },
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.x"
  }
}
